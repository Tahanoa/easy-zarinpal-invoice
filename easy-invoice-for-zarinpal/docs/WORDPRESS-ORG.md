# WordPress.org Release Notes

Plugin name: **Easy Invoice for ZarinPal**  
Slug: `easy-invoice-for-zarinpal`  
Version: `1.5.0`  
Contributor: `tahanoa`  
Text domain: `easy-invoice-for-zarinpal`

The installable ZIP contains the plugin folder only. The separate SVN package contains:

- `trunk/` — current development/stable code.
- `tags/1.5.0/` — immutable release snapshot matching trunk for this first public release.
- `assets/` — WordPress.org directory icons and banners; these files are not part of the installable plugin ZIP.

Before the first submission, upload the installable ZIP through the WordPress.org plugin submission form. After approval and receipt of the SVN repository URL, copy the supplied `trunk`, `tags/1.5.0`, and `assets` contents into that repository and commit them.
